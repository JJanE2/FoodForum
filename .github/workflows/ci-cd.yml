name: SpringBoot CI/CD

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Decode application.yml from secret and save it to resources
        run: |
          echo "${{ secrets.APPLICATION_YML }}" | base64 --decode > src/main/resources/application.yml

      - name: Decode keystore.p12 from secret and save to it to resources
        run: |
          echo "${{ secrets.KEYSTORE_P12 }}" | base64 --decode > src/main/resources/keystore.p12
  
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle (bootJar)
        run: ./gradlew bootJar

      - name: Check JAR file location
        run: ls -alhR build/libs

      # - name: Upload JAR to EC2
      #   uses: appleboy/ssh-action@v0.1.7
      #   with:
      #     host: ${{ secrets.EC2_HOST }}
      #     username: ${{ secrets.EC2_USER }}
      #     key: ${{ secrets.EC2_PRIVATE_KEY }}
      #     port: 22
      #     script: |
      #       scp -o StrictHostKeyChecking=no build/libs/foodforum-0.0.1.jar ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/home/${{ secrets.EC2_USER }}/
